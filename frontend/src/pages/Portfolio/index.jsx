// Dinamik Portfolyo Sayfası — Sürükle-bırak builder, GitHub/LinkedIn sync, sertifika yönetimi
// Dolduracak: Rabia & Melike (portfolioService + drag-drop kütüphanesi)

import './style.css';

function Portfolio() {
  return (
    <div className="portfolio-page">
      <section className="portfolio-builder">
        {/* Sürükle-bırak bölüm düzenleyici */}
      </section>

      <section className="integrations">
        {/* GitHub / LinkedIn bağla butonları */}
      </section>

      <section className="certificates-section">
        {/* Sertifika yükle + OCR doğrulama sonucu */}
      </section>

      <section className="portfolio-preview">
        {/* Anlık önizleme */}
      </section>
    </div>
  );
}

export default Portfolio;
